[core]
	pager = delta

[interactive]
	diffFilter = delta --color-only

[delta]
	navigate = true
	side-by-side = true
	line-numbers = true
	# Keep theme readable on Catppuccin terminals
	syntax-theme = ansi

[merge]
	conflictstyle = zdiff3

[diff]
	colorMoved = default
